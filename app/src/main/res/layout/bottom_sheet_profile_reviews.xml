<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_bottom_sheet_rounded"
    android:orientation="vertical">

    <!-- Fixed Header Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Drag handle -->
        <View
            android:layout_width="@dimen/bottom_sheet_drag_handle_width"
            android:layout_height="@dimen/bottom_sheet_drag_handle_height"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="@dimen/spacing_sm"
            android:background="@drawable/bg_bottom_sheet_handle" />

        <!-- Header Row: Title + Close Button -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/spacing_xl"
            android:paddingTop="@dimen/spacing_lg"
            android:paddingBottom="@dimen/spacing_md">

            <!-- Title -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start|center_vertical"
                android:layout_marginEnd="48dp"
                android:textColor="@color/text_primary"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="Reviews for Mia Explorer" />

            <!-- Close Button -->
            <ImageButton
                android:id="@+id/btnClose"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="end|center_vertical"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/cd_close"
                android:src="@drawable/ic_close"
                app:tint="@color/text_secondary" />
        </FrameLayout>

        <!-- Filter Chips - Horizontally Scrollable -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:paddingHorizontal="@dimen/spacing_xl"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/chipsContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingVertical="@dimen/spacing_sm">

                <!-- All Filter Chip -->
                <TextView
                    android:id="@+id/chipAll"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_filter_chip_active"
                    android:gravity="center"
                    android:paddingHorizontal="20dp"
                    android:text="@string/filter_all"
                    android:textColor="@color/white"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <!-- 5 Star Filter -->
                <TextView
                    android:id="@+id/chip5Star"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/bg_filter_chip_inactive"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:text="@string/filter_5_star"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

                <!-- 4 Star Filter -->
                <TextView
                    android:id="@+id/chip4Star"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/bg_filter_chip_inactive"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:text="@string/filter_4_star"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

                <!-- 3 Star Filter -->
                <TextView
                    android:id="@+id/chip3Star"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/bg_filter_chip_inactive"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:text="@string/filter_3_star"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

                <!-- 2 Star Filter -->
                <TextView
                    android:id="@+id/chip2Star"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/bg_filter_chip_inactive"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:text="@string/filter_2_star"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

                <!-- 1 Star Filter -->
                <TextView
                    android:id="@+id/chip1Star"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/bg_filter_chip_inactive"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:text="@string/filter_1_star"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

            </LinearLayout>
        </HorizontalScrollView>

    </LinearLayout>

    <!-- Reviews List - Scrollable -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:paddingTop="@dimen/spacing_sm"
        android:paddingBottom="@dimen/spacing_xl"
        tools:listitem="@layout/item_review_card" />

    <!-- Empty State -->
    <LinearLayout
        android:id="@+id/layoutEmptyState"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/spacing_xxl"
        android:visibility="gone">

        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:alpha="0.4"
            android:src="@drawable/ic_star_empty"
            app:tint="@color/text_hint" />

        <TextView
            android:id="@+id/tvEmptyTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_lg"
            android:gravity="center"
            android:text="@string/empty_reviews_title"
            android:textColor="@color/text_secondary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvEmptySubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:gravity="center"
            android:text="@string/empty_reviews_subtitle"
            android:textColor="@color/text_hint"
            android:textSize="14sp" />

    </LinearLayout>

</LinearLayout>

